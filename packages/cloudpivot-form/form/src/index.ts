/*
禁止修改!此文件是产品代码的一部分，后续可能变更或者不再开放。
若有问题，请参考前端相关二开文档。
*/
import * as renderer from './renderer';

import * as runtime from './runtime';

import * as schema from './schema';

export { renderer, runtime, schema };
